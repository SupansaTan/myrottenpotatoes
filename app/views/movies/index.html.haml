%h1 All Movies
 
%table#movies
  %thead
    %tr
      %th.text_center #
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info

  %tbody
    - @sort_movies.each_with_index do |movie, index|
      %tr
        %td.text_center= index
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path