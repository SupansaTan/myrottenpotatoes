#login
  - if @current_user
    %p.welcome  Welcome, #{@current_user[:name]}!
    = link_to 'Log Out', destroy_user_session_path, method: :delete
  - else
    %p.login= link_to 'Sign in with Facebook', user_facebook_omniauth_authorize_path