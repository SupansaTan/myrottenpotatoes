#login
  - if @current_user
    %p.welcome  Welcome, #{@current_user[:name]}! 
    %p.logout= button_to 'Sign Out', destroy_user_session_path, method: :delete 
  - else
    %p.login= button_to 'Sign in with Facebook', user_facebook_omniauth_authorize_path
